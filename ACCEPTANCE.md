# ACCEPTANCE

## Phase 1 - "전체 폴더 구조 및 모듈 설계" 완료 기준

1. 루트 구조(앱/패키지/운영/문서)가 정의되어 있어야 한다.
2. 각 계층(Domain/Application/Infrastructure/Apps) 책임이 명확해야 한다.
3. 핵심 유스케이스 4종(실시간 수집/섹터 집계/조회/일마감 재계산) 경계가 정의되어야 한다.
4. 의존성 방향과 금지 규칙이 문서화되어야 한다.
5. 확장 포인트(브로커, 저장소, 데이터 소스)가 설계에 반영되어야 한다.

6. 지수 기준 정의가 아래 식/규칙대로 문서화되어 있어야 한다.
   - 전체 기간: `2022-01-03 ~ today`
   - `RawIndex` 누적 시작점: `2022-01-03`
   - `Index_2022(t) = RawIndex(t) / RawIndex("2022-01-03") * 1000`
   - `Index_2025(t) = RawIndex(t) / RawIndex("2025-01-02") * 1000`
   - `2025-01-02` 부재 시 가장 가까운 이전 거래일 기준점 사용
